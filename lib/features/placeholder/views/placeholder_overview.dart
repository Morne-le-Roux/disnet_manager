import 'package:flutter/material.dart';

class PlaceholderOverview extends StatelessWidget {
  const PlaceholderOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Placeholder Overview"),
    );
  }
}
